greeting:
  value: yo from yaml
spring:
  application:
    name: resttwo
eureka:
  client:
    service-url:
        defaultZone: http://localhost:8761/eureka
  instance:
    preferIpAddress: true
    lease-renewal-interval-in-seconds: 10